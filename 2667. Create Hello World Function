/**
 * @return {Function}
 */
 
var createHelloWorld = function () {
    return function (...args) {
        return "Hello World";
    }
};

createHelloWorld()();  //using currying to avoid multiple lines of code.

/**
 * const f = createHelloWorld();
 * f(); // "Hello World"
 */
